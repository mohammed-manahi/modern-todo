import Layout from "../ui/Layout.jsx";

import {Button, Container, Group, Title, Text} from "@mantine/core";
import NotFoundContent from "../ui/NotFoundContent.jsx";

function NotFound() {
    return (
        <Layout>
          <NotFoundContent/>
        </Layout>
    );
}

export default NotFound;